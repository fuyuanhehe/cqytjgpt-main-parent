info:
  description: 重庆交巡警源头监管项目大屏展示api模块

server:
  port: 12001

spring:
  application:
    name: cqytjgpt-screen-api
  profiles:
    active: dev
  cloud:
    config:
      enabled: true
      discovery:
        enabled: true
        service-id: cqytjgpt-eureka-config
      fail-fast: true
      label: master